<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<ContentDialog x:Class="TerminalApp.ProjectOrganizerDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               CloseButtonText="취소"
               PrimaryButtonText="저장"
               Title="프로젝트 관리"
               mc:Ignorable="d">

    <ContentDialog.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="ContentDialogSmokeBrush" Color="#99000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="ContentDialogSmokeBrush" Color="#99000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="ContentDialogSmokeBrush" Color="#99000000" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </ContentDialog.Resources>

    <StackPanel x:Name="OrganizerContentPanel"
                MinWidth="460"
                Spacing="10">

        <TextBlock FontSize="11"
                   Opacity="0.65"
                   Text="프로젝트를 드래그해서 순서를 바꾸고, 폴더로 묶을 수 있습니다."
                   TextWrapping="Wrap" />

        <!-- New folder row -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewFolderNameBox"
                     Grid.Column="0"
                     GotFocus="_OnTextBoxGotFocus"
                     PlaceholderText="새 폴더 이름" />
            <Button x:Name="AddFolderButton"
                    Grid.Column="1"
                    Margin="6,0,0,0"
                    Click="_AddFolderClicked"
                    Content="폴더 추가" />
        </Grid>

        <!-- Folder + project list -->
        <ScrollViewer MaxHeight="400"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="OrganizerListPanel"
                        Spacing="4" />
        </ScrollViewer>

    </StackPanel>
</ContentDialog>
