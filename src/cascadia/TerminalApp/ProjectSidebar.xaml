<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<UserControl x:Class="TerminalApp.ProjectSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:TerminalApp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Width="250"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0"
              Height="40"
              Padding="12,0,8,0"
              Background="{ThemeResource NavigationViewExpandedPaneBackground}">
            <TextBlock FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="PROJECTS"
                       VerticalAlignment="Center" />
            <Button x:Name="AddProjectButton"
                    Width="28"
                    Height="28"
                    Padding="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Click="_AddProjectClicked"
                    ToolTipService.ToolTip="Add Project">
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                          FontSize="12"
                          Glyph="&#xE710;" />
            </Button>
        </Grid>

        <!-- Project list -->
        <ScrollViewer Grid.Row="1"
                      Background="{ThemeResource NavigationViewExpandedPaneBackground}"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ProjectListPanel"
                        Padding="6,4,6,8"
                        Spacing="2" />
        </ScrollViewer>
    </Grid>
</UserControl>
